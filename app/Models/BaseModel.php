<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

/**
 * Shared base model — can be extended to add common logic (soft deletes, scopes, etc).
 */
class BaseModel extends Model
{
    //
}
